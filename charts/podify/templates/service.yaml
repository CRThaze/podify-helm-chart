apiVersion: v1
kind: Service
metadata:
  name: {{ include "podify.fullname" . }}
  labels:
    {{- include "podify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "podify.selectorLabels" . | nindent 4 }}
