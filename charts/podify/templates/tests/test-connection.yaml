apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "podify.fullname" . }}-test-connection"
  labels:
    {{- include "podify.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "podify.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
